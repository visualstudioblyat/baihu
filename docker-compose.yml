# Baihu Docker Compose Example
# 
# Quick start:
#   1. Copy this file and set your API key
#   2. Run: docker-compose up -d
#   3. Access gateway at http://localhost:3000
#
# For more info: https://github.com/visualstudioblyat/baihu

services:
  baihu:
    image: baihu/baihu:latest
    # Or build locally:
    # build: .
    container_name: baihu
    restart: unless-stopped
    
    environment:
      # Required: Your LLM provider API key
      - API_KEY=${API_KEY:-}
      # Or use the prefixed version:
      # - BAIHU_API_KEY=${BAIHU_API_KEY:-}
      
      # Optional: LLM provider (default: openrouter)
      # Options: openrouter, openai, anthropic, ollama
      - PROVIDER=${PROVIDER:-openrouter}
      
      # Optional: Model override
      # - BAIHU_MODEL=anthropic/claude-sonnet-4-20250514
      
    volumes:
      # Persist workspace and config
      - baihu-data:/data
      
    ports:
      # Gateway API port
      - "3000:3000"
    
    # Health check
    healthcheck:
      test: ["CMD", "baihu", "doctor"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

volumes:
  baihu-data:
